var jewlsAndStone=function(jewel,stone){
   let jSet=new set();
   let count=0;
   for(let i=0;i<jewel.length;i++){
       jSet.add(j[i]);
   }
   
   for(let j=0;j<stone.length;j++){
       if(jSet.has(stone[i]){
	     count++;
	   }
   }
   
   return count;
}
